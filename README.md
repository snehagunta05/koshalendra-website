# Koshalendra Skill Academy website (Flask)

    python -m venv venv
    source venv/bin/activate        # Windows: venv\Scripts\activate
    pip install -r requirements.txt
    python app.py                   # open http://127.0.0.1:5000

Enquiries are saved in academy.db. View them at /admin?key=YOUR_ADMIN_KEY
Set SECRET_KEY and ADMIN_KEY as environment variables before going live.
